Assignment 1 (read-json-data) -> 
Write a Nodejs server that listen on port 3001 and which will read person.json and return a response in JSON format. Person.json content as follows: 
[{ PersonID:1, PersonName:”Rita”, PersonAddress:”Ahmedabad” }, { PersonID:2, PersonName:”Reema”, PersonAddress:”Calcutta” }]

Assignment 2
Write a Nodejs server that serves as a RESTFUL API that takes two parameters in a GET call and produces their sum. http://localhost:3001/product?param1=5¶m2=10

Assignment 3
Write a Nodejs server that serves as a RESTFUL API that accepts a string as an input name and returns the first vowel character from the string.

Assignment 4
Write a Nodejs server that serve as a RESTFUL API that accepts a file content and writes them to the disk. http://localhost:3001/upload